<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.care.mybatis.productMapper.productMapper">
	<!--  남자 신발 전체 검색 -->
	<select id="AllListMen" resultType="Pdto">
		select * from product where (gender='남자' or gender='공용') ORDER BY joindate, codename  
	</select>
	<!--  남자 카테고리별 신발 전체 검색 -->
		<select id="AllListMenCategory" resultType="Pdto">
		select * from product where (gender='남자'or gender='공용') and category=#{category} ORDER BY joindate, codename
	</select>
	<!--  전체 총 페이지 -->
		<select id="countProduct" resultType="int">
		select count(*) from product;		
		</select>
		
	<!-- 신발 검색 -->
		<select id="searchCode" resultType="Pdto">
		select * from product where codename like '%'|| #{codename} ||'%'		
		</select>
		
	<!-- 여자 신발 전체 검색 -->
	<select id="AllListWomen" resultType="Pdto">
		select * from product where (gender='여자' or gender='공용') ORDER BY joindate, codename  
	</select>
		
	<!-- 여자 카테고리별 신발 전체 검색 -->
		<select id="AllListWomenCategory" resultType="Pdto">
		select * from product where (gender='여자'or gender='공용') and category=#{category} ORDER BY joindate, codename
	</select>
	
	<!-- Kids 신발 전체 검색 -->
	<select id="AllListKids" resultType="Pdto">
		select * from product where gender='키즈' ORDER BY joindate, codename  
	</select>
		
	<!-- Kids 카테고리별 신발 전체 검색 -->
		<select id="AllListKidsCategory" resultType="Pdto">
		select * from product where gender='키즈' and category=#{category} ORDER BY joindate, codename
	</select>
</mapper>

